<view class="titlecss"></view>
<view class='forcss' wx:for="{{items}}" wx:key="{{item.title}}" data-id="{{item.title}}" bindtap='clickbutton'>
  <view class='contentcss'>
    {{item.name}}
  </view>
</view>